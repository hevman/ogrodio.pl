/** Horizontal padding shared by the service site and shop. */
export const siteGutter = "px-6 2xl:px-8";

/** Content max-width shared by the service site and shop. */
export const siteContainer = "mx-auto w-full max-w-[1680px]";

/** Container + gutter on one element. */
export const siteShell = `${siteContainer} ${siteGutter}`;
